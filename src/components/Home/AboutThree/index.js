import React from 'react';
import Slider from './Slider.js';

const AboutThree = () => {
    return (
        <div style={{ position: 'relative', width: '100%', height: '100vh', backgroundColor: '#000', display: 'flex', justifyContent: 'center', alignItems: 'flex-end'}}>
            <Slider />    
        </div>
    );
};

export default AboutThree;
